@model IEnumerable<Uc_10_Ryan_Relacionamentos_Descricao_00501.Models.Marca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Cadastrar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MarcaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.MarcaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.MarcaId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
